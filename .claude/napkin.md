# Napkin Runbook

## Curation Rules
- Re-prioritize on every read.
- Keep recurring, high-value notes only.
- Max 10 items per category.
- Each item includes date + "Do instead".

## Execution & Validation (Highest Priority)
1. **[2026-05-10] Public site deploys from `site/` via GitHub Actions**
   Do instead: edit `site/*`, push to `main`, then verify `https://nogueirathiago.github.io/viagem-pagamentos/` and the latest Pages workflow.
2. **[2026-05-10] Admin writes directly to GitHub through user-provided token**
   Do instead: keep tokens out of the repo; the admin page uses a pasted fine-grained token to update `site/data.json`.
3. **[2026-05-10] Avoid stale payment data in public panel**
   Do instead: fetch `data.json` with a cache-busting query and `cache: "no-store"` so removals and edits appear after deploy.
4. **[2026-05-10] WhatsApp preview is generated by Python**
   Do instead: update `render_social_preview()` in `scripts/generate_panel.py`, run `python3 scripts/generate_panel.py`, and bump the `og:image` query string when the preview design changes.
5. **[2026-05-08] Generate real sample outputs after scaffold**
   Do instead: always run the local generator once and verify the expected files are produced before finishing.

## Shell & Command Reliability
1. **[2026-05-10] Remote may change from admin saves**
   Do instead: if push is rejected, `git fetch origin`, inspect remote changes, rebase onto `origin/main`, and preserve payment updates from `site/data.json`.
2. **[2026-05-08] Empty workspaces need explicit structure**
   Do instead: create a minimal file layout with clear data, script, output, and usage docs so future updates stay simple.

## User Directives
1. **[2026-05-10] Dashboard aesthetic preference**
   Do instead: avoid dominant blue gradients; use progress-driven colors from red at 0% to green at 100%, and keep saldo cards in the same bold metric-card style.
2. **[2026-05-10] Event label**
   Do instead: use `Viagem réveillon 2027` as the visible trip/group label.
3. **[2026-05-08] WhatsApp-friendly payment tracking**
   Do instead: prefer legible portrait layouts, concise Portuguese labels, and a paired text fallback for easy reposting.
